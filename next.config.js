/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: 'out',
    basePath: '/prueba-alex',
    images: {
        unoptimized: true,
    },
};

module.exports = {
    output: 'export',
    distDir: 'out',
    basePath: '/prueba-alex'
};
